import { Injectable } from '@angular/core';

/**
 * For complex applications we can use Akita or NgRx to persist the state of the application
 */
@Injectable({ providedIn: 'root' })
export class LocalStateService {}
